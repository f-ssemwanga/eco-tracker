import { Features } from "../components/Features";
import {AboutUs} from "../components/AboutUs";

export const Home = () => {
    return (
        <div>
        <Features/>
        <AboutUs/>
        </div>

    )
}